# Project Rules

## 用户确认优先

- 用户已确认的方向、页面、文案口径、导航层级和数据边界，是本项目唯一基准。
- 不得用“我觉得更完整”“看起来专业”替代用户已确认的版本。
- 每个页面开做前必须先给高保真图，用户确认后再实现。
- 正式环境站点代码部署 / promote production 必须用户当次手动通知并明确授权；默认只允许部署 dev，不能因为 dev 验证通过、定时任务、环境变量或历史授权自动 promote prod。
- 已确认例外：`美股数据与产品刷新 v2` 可在 DB-first 验证、release gate、覆盖检查、DB-only 打包和 dev 部署通过后，自动准备本轮 `data/product.db`；若要部署到 prod，只允许通过 `scripts/deploy_prod_data.sh`，且在存在 `Open 持仓`运行表时必须当次人工授权，不得 promote prod 站点代码、后台、用户库或其它模块。
- `Open 持仓`的 `open_portfolio_trades`、`open_portfolio_symbol_rules` 及其计算结果属于生产运行数据。任何写入、删除、迁移、重算、导入、恢复或替换，都必须由用户在当次明确人工授权；普通代码发布不得复制、覆盖或触碰 `/opt/dongbimao-prod/data/product.db`，且必须在发布前后校验其未变化。
- 课程图片、视频的盘点、压缩、转码和迁移必须先在 local / dev 完整验证。任何读取以外的 prod 媒体操作、prod 数据库字段更新、prod COS 上传/覆盖/删除或链接切换，都必须由用户在当次明确人工授权；dev 验证通过不等于获得 prod 授权。

## 交付汇报格式

- 汇报必须简短，避免大段解释。
- 默认只写：做了什么、验证结果、是否已部署、后续任务。
- 后续任务必须带上，不让用户再追问“下一步干嘛”。
- 复杂背景只在用户明确要求时展开。

## 需求处理硬性流程

- 拿到任何新需求，必须先从产品经理视角判断：这个需求解决谁的问题、用户当前路径是什么、用户下一步要做什么、是否会增加操作负担。
- 开始设计或写代码前，必须先问自己：成熟产品通常怎么处理这个场景；当前方案是否合理；是否足够专业；用户体验是否顺；有没有更少字、更少点击、更少干扰的做法。
- 后台管理需求必须优先参考成熟后台范式：Ant Design、Element Plus、飞书/企业微信后台、Stripe/Notion 类控制台的信息密度、反馈方式、表格操作和弹窗结构；不得凭空自创粗糙组件。
- 金融/交易/权限/课程/用户管理等高风险或高频操作，必须先明确操作前、操作中、操作成功、操作失败、空状态、危险操作确认这 6 种状态；缺任何一种都不能进入实现。
- 方案阶段必须先给出“为什么这样设计”的一句话判断，再给高保真图或结构图；不能直接堆 UI，也不能只改颜色、圆角、字号冒充产品升级。
- 如果现有方案字多、按钮多、提示重、层级乱、弹窗混用、操作路径绕，必须先降噪：删重复文案、合并重复操作、统一入口、统一反馈，再考虑加功能。
- 用户确认设计前不得实现；用户指出“不专业 / 不合理 / 体验不好 / 去学习”时，必须停止原方向，重新参考成熟产品范式后再出新方案。
- 实施时必须优先抽象到共享规范：按钮、表格、弹窗、toast、表单校验、空状态、危险确认、权限提示等只能有一套项目级表达，不能每页各写各的。
- 交付前必须做产品自检并在心里过一遍：是否解决真实任务、是否符合成熟产品习惯、是否少字少干扰、是否状态完整、是否前后台边界清楚、是否没有未确认口径。

## 前台产品页面文案规则

- 前台是给 C 端用户看的产品，不是给开发、数据、运营看的说明文档。
- 页面默认不写解释性语言、使用说明、字段来源、设计意图、数据口径说明。
- 不重复表达同一个含义：例如顶部标题、模块标题、卡片标题不能反复出现同一词。
- 不展示内部状态、调试口径、覆盖数量、字段完整度、数据来源、接口来源、待补字段等后台信息。
- 文案要像产品界面，不像方案汇报；能用数据、标题、按钮、表格结构表达的，不再额外解释。
- 如果必须解释复杂指标，只能用轻量问号或 hover 提示，不在页面主体铺说明文字。

## 数据与功能边界

- 只展示现有 DB / API 能支撑的数据。
- 没有的数据、不能计算的数据，不得为了页面好看编造、推断或占位成真实结论。
- 如果数据缺失，优先隐藏对应模块或弱化展示，不向用户暴露“暂无字段 / 待接入 / 字段来源”等内部信息。
- 不得把“看起来合理”的人工分组、统计数字、趋势结论、状态标签当成真实数据展示；除非能在 DB / API / 用户提供内容中查到来源。

## 前后端工程规范

- dev / prod 必须隔离运行：dev 前台只能访问 dev API 和 dev DB，prod 前台与 admin 后台只能访问 prod API 和 prod DB；不得为了省事让 dev 复用生产数据库或生产 API。
- Git 必须保持可回滚基线：源码、脚本、测试、项目规则进 Git；构建产物、缓存、截图、临时包、密钥、运行时 DB 不进 Git。
- `master` 只放已验证、可部署的稳定代码；新需求和修复必须先开 `codex/...` 分支，确认后再合回 `master`。
- 代码改动部署前必须先形成 Git 提交：检查通过、提交完成、再部署；不得在部署后由脚本偷偷自动提交未审阅改动。
- 每次需求优先小改动、小提交；不得把无关重构、数据刷新、页面改版和部署脚本混在同一个提交里。
- 提交或部署前至少跑 `npm run check`；如果只改数据脚本，至少跑对应脚本的 `bash -n` / `py_compile`。
- 同一业务含义只能有一个共享出口：趋势信号、权限遮罩、涨跌色、时间格式、金额格式、会员状态等必须抽成组件 / 工具函数 / 统一 CSS 类。
- 不得为某个页面单独复制一套同类 UI；页面 CSS 只处理布局和局部间距，通用视觉必须走共享类。
- 新增或重构样式时必须同步清理被替代的旧规则、重复选择器和临时覆盖；禁止用后置 CSS 覆盖长期堆补丁。
- 前端类型以 `main-web/src/api.ts` 为准，后端接口字段变更必须同步类型和最小验证。
- 时间统一用 `YYYY-MM-DD HH:mm:ss`、24 小时制；前后端都不能临时各写各的格式化。
- 修 bug 优先找共用入口，一处修复覆盖所有调用方；不要在单个页面用覆盖样式掩盖根因。

## 认证与邮件安全

- 忘记密码必须走邮件重置链接，不返回或暴露原密码。
- 忘记密码接口必须限频：同 IP 每小时最多 5 次，同邮箱每小时最多 3 次；返回文案必须统一，不能暴露邮箱是否注册。
- 重置 token 只存 hash，不存明文；默认 30 分钟过期，用完即失效。
- 邮件发送失败不能把技术错误直接展示给用户；前台只提示“如果邮箱存在，我们会发送重置链接”。
- 邮件服务未配置完成前，不上线可点击的忘记密码入口，避免用户走进无结果流程。

## 设计流程

- 出图前必须先完成产品判断：页面主任务、目标用户、主要路径、关键状态、风险点、是否有重复/无用字段。
- 出图前必须先参考成熟产品的通用处理方式，并说明本项目采用其中哪种范式；不能先凭感觉画，再事后解释。
- 每个页面先出图：先选用或调整已有高保真图，再让用户确认。
- 用户确认前不得实现、部署或扩大到其它页面。
- 高保真图只是确认产物：未确认、已替代、或用户明确不要的 mockup 源文件和截图必须同轮清理，不得长期堆在工作区。
- 所有产品设计必须先参考成熟产品的通用范式，再结合本项目数据和用户路径落地；不得凭空发散自创难懂组件。
- 如果用户指出“没变化 / 不专业 / 不要自己想 / 去学”，必须先重新参考成熟产品范式并出新图，不能继续在原页面上小修小补。
- 参考成熟产品不是照抄视觉皮肤，而是学习信息密度、列表层级、表格列宽、阅读节奏、权限遮罩和下一步路径。
- 金融数据页优先采用成熟终端式结构：紧凑标题、清晰表格、固定列宽、红绿涨跌、可扫描的数值层级；不要用大块文案解释页面。
- 内容阅读页优先采用成熟内容产品结构：标题、时间、栏目、正文、标签、上一篇/返回；列表页只露必要摘要，不把详情页内容摊满。
- 付费/权限提示参考成熟内容产品的轻量预览、渐隐遮罩、克制标识；不要做突兀、丑陋、临时贴片式的大锁。
- 权限遮罩只遮正文或核心数据，不遮到页面结构失真；股票代码/名称等核心线索按用户要求处理，不能擅自漏出或隐藏。
- 白版视觉语言优先沿用后台：浅灰底、白色面板、系统字体、清晰表格、轻阴影、蓝色主色。
- 前台字号和间距默认克制：避免超大标题、粗重字体、大面积留白和一屏只有少量信息；以成熟网站的信息密度为参考。
- 页面要从用户路径出发：用户打开后先看什么、下一步点哪里、为什么值得继续看。
- 出图前必须先明确：这是前台用户页、后台管理页、详情页、列表页、权限页还是工具数据页；不同页面不能混用模块。

## 全项目产品设计方向

- 这不是只针对首页，而是整个项目的产品设计规则。
- 前台主站、后台管理、登录注册、权限提示、内容编辑、导航结构，都必须按产品化体验统一设计。
- 前台用户页面和后台管理页面必须分开设计、分开出图、分开实现；不得把后台内容管理、用户管理、管理员模块放进前台用户可见页面。
- 前台整体倾向切换为后台同款白版风格；后台继续保持清爽、专业、易管理。
- 所有页面都不是数据拼盘，也不是说明文档；每个页面必须有明确主任务、主要信息、下一步动作。
- 美股热点风向标、股票机会跟踪榜单、市场与资金、股票库、美股重点财经前瞻、用户权限、后台内容管理、后台用户管理，都要逐页按确认图实施。
- 已确认的导航和功能边界不得擅自改变；新增、隐藏、置灰、改名，都必须先让用户确认。

## 前台已确认功能边界

- 前台主导航先围绕：`首页`、`美股热点风向标`、`股票机会跟踪榜单`、`股票库`、`美股重点财经前瞻`。
- `Open 持仓参考`、`论坛讨论区`按用户确认状态处理；未正式开放时只在导航中按待上线或权限状态表达。
- `交易实战课程`暂不放到前台主导航。
- 工具数据类功能放在下面，不抢主功能位置；其中`热门股票板块`对应`市场与资金`。
- 后台管理只出现在独立后台，不进入普通用户前台页面。
- 会员权限是访问控制，不是内容模块；不得在普通内容页硬塞“会员内容/升级模块”。只有用户点击无权限内容时，才用遮罩、预览或联系开通承接。
- 未登录用户点击前台任何可交互入口（导航、搜索、列表、详情、分页、筛选、按钮、行点击）都应先提示注册/登录；不要让游客自由浏览交互路径。

## 已犯错通识与自检

- 不要凭空发散设计。用户要求“专业”时，先看成熟产品的通用做法，再结合本项目现有数据出图。
- 不要把“换颜色、调圆角、放大标题”当成产品化升级；真正升级必须改善信息密度、阅读层级、点击路径和权限承接。
- 不要反复做局部补丁。用户多次指出页面不行时，应整页重构高保真图，再确认实现。
- 不要把“全项目总览”误做成“前台用户可见页面”；总览图可以用于内部确认，前台图只能展示普通用户应看到的内容。
- 不要把后台模块塞进前台：内容管理、用户管理、管理员设置、发布状态、接口状态、字段来源、数据完整度都属于后台或内部信息。
- 不要把权限机制设计成页面内容模块；权限只在用户访问受限内容时出现，不占据正常内容位。
- 不要把锁、遮罩、会员提示做成突兀贴片；参考成熟内容产品，保持轻、准、可点，文案只保留“开通查看完整内容”这类短句。
- 不要把页面做成模块堆叠。每页先确定一个主任务，再安排入口、列表、详情、下一步动作。
- 不要重复标题和解释：导航名、页标题、卡片标题、正文不要反复表达同一个词或同一层意思。
- 不要为了“显得专业”加用户看不懂的缩写、口径、字段、状态；必须用中国散户能理解的人话表达。
- 不要在时间、涨跌幅、趋势信号这些基础表达上随意用缩写；时间用用户确认的 `YYYY-MM-DD HH:mm:ss` 和 24 小时制。
- 不要在 C 端页面展示“覆盖多少只、字段来源、数据状态、待补、后续开放、接口来源”等内部运营/开发语言。
- 不要把未确认导航、未开放功能、临时想法直接放进主导航；新增、隐藏、置灰、改名都先确认。
- 不要用假数据、推测数据、无法计算的数据做视觉填充；缺数据时隐藏或降级展示。
- 不要在用户已经确认“先给图”的情况下直接实现；每个页面先出高保真图，确认后再写代码。
- 不要只做 UI 皮肤。产品设计必须同时回答：用户打开先看什么、为什么要点、点进去看到什么、无权限怎么承接。
- 不要在列表页、详情页、首页之间混淆职责：列表负责筛选和对比，详情负责阅读和深入，首页负责今日路径。
- 不要把详情页和列表页做成同一种排版；详情页要服务阅读，列表页要服务扫描和选择。
- 不要为了“丰富”把同一批内容在首页、栏目页、卡片区重复展示；同一信息只保留一个最有价值的位置。
- 每次交付前先自检：前后台是否混用、是否有解释性废话、是否展示了内部信息、是否有无依据数据、是否重复表达、是否符合用户已确认导航。
